/**
 * Plugin para inicializar el tema al cargar la aplicación
 * Simple persistencia en localStorage
 */

export default defineNuxtPlugin(() => {
  // El tema se persiste en localStorage automáticamente
  // No se necesita lógica especial aquí
});
